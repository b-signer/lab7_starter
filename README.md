1. I would rather put it within a Github action that runs whenever the code is pushed, because then it is clear for anyone within the organization what is going wrong and prevents people from actually pushing code that doesn't work.
2. No
3. Navigation mode reloads the page from scratch; Snapshot mode evaluates
the page how it currently exists without reloading.
4. We could add an HTML `lang` tag, we could write a meta-description for the site, and use efficient cache lifetimes.




